# README

```bash
# Get the image ID
docker image ls

# Tag the image
docker tag IMAGE_ID ncalteengithub.jfrog.io/docker/docker-example:1.0.0

# Push the image
docker push ncalteengithub.jfrog.io/docker/docker-example:1.0.0
```
